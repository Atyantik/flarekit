{
  "name": "@apps/web",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "npm run setup && astro dev",
    "build": "npm run setup && astro build",
    "preview": "npm run setup && wrangler pages dev ./dist",
    "astro": "npm run setup && astro",
    "setup": "node ../../scripts/generate-wrangler.json.js && wrangler types"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/cloudflare": "^12.1.0",
    "@services/database": "*",
    "astro": "^5.1.1",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "prettier-plugin-astro": "^0.14.1",
    "tsx": "^4.19.2"
  }
}
